import { Box, Button, Center, Flex, IconButton, Spacer, Text } from "@chakra-ui/react";

const HotSection = () => {
  return (
    <Box flex="1">
      <Text>ini HotScreen</Text>
    </Box>
  );
};

export default HotSection;
