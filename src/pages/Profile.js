import { Box, Button, Center, Flex, IconButton, Spacer, Text } from "@chakra-ui/react";
import MenubarDemo from "../components/menubar/menubar";
import NavigationMenuDemo from "../components/navmenu/Navbar";
import { FaceIcon, ImageIcon, SunIcon, PersonIcon } from '@radix-ui/react-icons'


const Profile = () => {
  return (
    <Box flex="1">
      <Text>ini profle</Text>
    </Box>
  );
};

export default Profile;
